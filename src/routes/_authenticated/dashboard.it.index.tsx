import { createFileRoute } from "@tanstack/react-router";import { useEffect,useState } from "react";import { useServerFn } from "@tanstack/react-start";import { Users,Building2,Activity,DollarSign } from "lucide-react";import { DashboardShell,StatCard,Panel } from "@/components/dashboard/DashboardShell";import { RoleGate } from "@/components/dashboard/RoleGate";import { getAdminDashboard } from "@/lib/dashboard-data.functions";
export const Route=createFileRoute("/_authenticated/dashboard/it/")({component:()=> <RoleGate allow={["it"]} exclusive><Page/></RoleGate>});
function Page(){const fn=useServerFn(getAdminDashboard),[s,setS]=useState<any>({});useEffect(()=>{fn().then(setS)},[]);return <DashboardShell title="IT Dashboard" subtitle="Full platform administration and system controls" role="it"><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4"><StatCard icon={Users} label="Users" value={String(s.users||0)}/><StatCard icon={Building2} label="Properties" value={String(s.properties||0)}/><StatCard icon={Activity} label="Open tasks" value={String(Number(s.open_services||0)+Number(s.open_visits||0))}/><StatCard icon={DollarSign} label="Commission MTD" value={`USD ${Number(s.month_commission||0).toLocaleString()}`}/></div><div className="mt-6"><Panel title="IT control center" subtitle="Use System Health to monitor MySQL, SMS, Resend, R2 and NIDA and to enable/disable platform features."><p className="text-sm text-noir/60">Authentication, business data and role permissions in this build are MySQL-backed.</p></Panel></div></DashboardShell>}
